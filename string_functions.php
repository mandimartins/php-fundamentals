<?php

    # substr()
    # Returns a portion of a string

    $output = substr("Hello", 1); // start off by the 2º letter => the number is actually a index like array

    // echo $output; // prints out ello

    #Returns the length of a string
    $strlen("Hello World!");

    #Finds the position of the first occurence of a sub string
    strpos("Hello World!", "o"); //prints out 4

    #Finds the position of the last occurence of a sub string
    strrpos("Hello World!", "o"); //prints out 7

    #trim()
    #Strips whitespace

    #strtoupper("My simple string")
    #Makes everything uppercase

    #ucwords()
    #Capitalize every word

    #str_replace()
    #Replace all occurences of a search string with a replacement
